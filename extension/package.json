{
  "name": "otpilot",
  "version": "1.0.0",
  "description": "TOTP authenticator for Chrome and Edge — auto-detects 2FA setup pages, saves secrets in one click, and auto-fills codes on login.",
  "main": "content.js",
  "type": "module",
  "scripts": {
    "test": "playwright test",
    "test:ui": "playwright test --ui",
    "test:report": "playwright show-report"
  },
  "author": "Alberto Paparelli",
  "license": "GPL-3.0-only",
  "homepage": "https://otpilot.app",
  "devDependencies": {
    "@playwright/test": "^1.60.0"
  }
}
